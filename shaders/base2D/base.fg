#version 330 core
varying vec2 coord;

void main(){
    vec2 st = coord;
    vec3 color = vec3(0.0);
    st = (st+1.0)*0.5;
    color = vec3(st.x,st.y,1.0);
    gl_FragColor = vec4(color.x,color.y,.0,1.0f);
}
